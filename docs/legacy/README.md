# Documentation legacy

Guides écrits **avant la migration monorepo** (juillet 2026). Ils référencent les anciens chemins (`liveshop-backend/`, `web-client/`, `mobile-app/`) et des procédures de déploiement manuelles remplacées par le pipeline GitHub Actions (`.github/workflows/deploy.yml`).

Conservés pour référence (configuration Cloudinary, EmailJS, historique des choix push/realtime) — **ne pas suivre ces procédures telles quelles**.

Pour la documentation à jour : voir `README.md` à la racine et `docs/architecture/`.
